spring:
  application:
    name: us
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@${SERVICE_TYPE}?TNS_ADMIN=${WALLET_PATH}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
